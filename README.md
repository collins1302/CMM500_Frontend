# CMM500 Frontend



## How to run this application
1. Clone the repository
2. Install the required packages by running `npm install` in the project directory                                                                  
3. Start the application by running `npm run dev` in the project directory                                                        
4. Open your web browser and navigate to `http://localhost:5173` to view the application
## Features of the application

